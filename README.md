# Network-Inference-with-Distributional-Nodes
Optimal-transport framework for network inference with distributional data. Nodes are probability distributions; edges capture similarity (Wasserstein distances) and directional dependencies (Wasserstein barycenters). Includes permutation-based validation and an application to European Social Survey data.
